---
# Tâches pour gérer les VRFs avec read_csv
- name: "Read CSV VRFs"
  read_csv:
    path: "{{ csv_dir }}/vrf.csv"
    delimiter: ','
  register: vrf_csv
  when: (csv_dir + '/vrf.csv') is file

- name: "Afficher les VRFs détectés"
  debug:
    msg: "VRFs à traiter: {{ vrf_csv.list | map(attribute='vrf') | list }}"
  when: vrf_csv is defined and vrf_csv.list is defined

- name: "{{ item.vrf }} - Créer VRF ACI"
  cisco.aci.aci_vrf:
    host: "{{ aci_hostname }}"
    username: "{{ aci_username }}"
    password: "{{ aci_password }}"
    validate_certs: "{{ aci_validate_certs | default(false) }}"
    annotation: ""
    use_ssl: true
    tenant: "{{ item.tenant }}"
    vrf: "{{ item.vrf }}"
    description: "{{ item.description | default(omit) }}"
    state: "{{ item.state | default(global_state) }}"
  loop: "{{ vrf_csv.list }}"
  when: 
    - vrf_csv is defined
    - vrf_csv.list is defined
    - item.vrf is defined
    - item.vrf | length > 0
    - item.tenant is defined
    - item.tenant | length > 0
  tags:
    - vrfs
    - aci_vrf
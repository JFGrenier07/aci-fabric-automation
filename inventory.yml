---
all:
  hosts:
    localhost:
      ansible_connection: local
  vars:
    # Variables de connexion ACI - Fabric de test
    aci_hostname: "192.168.0.245"
    aci_username: "admin"
    aci_password: "truite79"
    aci_validate_certs: false
    
    # Fichier Excel de configuration
    excel_config_file: "aci_complete_standardized_fabric.xlsx"